# 秋招信息快捷填充助手

一款本地优先的 Chrome / Chromium Edge 浏览器扩展。它在当前网页右侧显示可拖动的悬浮面板，让你先选择网页表单控件，再手动点击“姓名”“手机号”“学校”等按钮填入信息。扩展只填写当前选中的控件，绝不会自动提交表单。

## 总体架构与关键技术选择

- 使用 Chrome Manifest V3，后台为 `background.js` service worker。
- 不声明常驻 `content_scripts`，用户点击扩展图标后才以 `chrome.scripting.executeScript` 向当前标签页注入 `content.js`。
- `content.js` 使用页面级单例标记避免重复注入；同一页面重复点击扩展图标只切换显示与隐藏。
- 悬浮面板放在 open Shadow DOM 中，面板样式来自扩展包内的 `content.css`，避免招聘网站样式互相污染。
- 个人信息存储在 `chrome.storage.local`。后台尽可能调用 `setAccessLevel({ accessLevel: "TRUSTED_CONTEXTS" })`，使普通页面中的 content script 无法直接读取本地配置。
- content script 只先获取字段名称、分组、是否已设置及敏感字段掩码；用户点击某个按钮后，才通过消息向 service worker 请求该字段的真实值。
- 会话 PIN 只保存 SHA-256 摘要，放在 `chrome.storage.session`；不会持久化明文 PIN。浏览器会话结束后锁定信息失效。
- 扩展不使用任何网络接口、CDN、远程脚本、遥测或统计服务。

## 功能

- 工具栏图标控制当前网页中的悬浮面板显示与隐藏。
- 面板支持拖动、折叠、展开、关闭、字段搜索、分组折叠。
- 自动记录最近一次点击的受支持可编辑元素并以蓝色边框高亮。
- 根据输入框类型、`name`、`id`、placeholder、ARIA 标签、附近 label 及常见中英文别名进行本地智能匹配；语义回退层会归一化编号、常见连接词、修饰词和职责类同义表达；高置信度匹配时自动展开分组、滚动到字段按钮并标记“智能匹配”，但不会自动填写。
- 面板提供分组快速导航栏，点击分组名称即可清除搜索过滤、展开并平滑滚动到对应分组；滚动字段列表时导航栏会同步标记当前分组。
- 显示目标元素选择器、placeholder 与附近 label 摘要。
- 支持 `input`、`textarea`、原生 `select` 和 `contenteditable`。
- 使用原生 value setter，并触发冒泡的 `beforeinput`、`input`、`change` 事件，兼容普通 HTML 及常见 React / Vue / Angular 受控文本框。
- 原生 `select` 先精确匹配 option `value`，再精确匹配显示文本，不做模糊选择。
- 日期和月份字段支持 `YYYY-MM-DD`、`YYYY/MM/DD`、`YYYY年MM月DD日`、`YYYY-MM`、`YYYY年MM月` 等输出格式。
- 提供插件级“撤销上次填写”，不会注册或拦截 `Ctrl+Z`。
- 提供成功、失败、不支持提示，约 2 秒后自动消失。
- 多信息方案、字段与分组的新增、编辑、删除和排序。
- JSON 导入、JSON 导出、恢复默认字段模板、双重确认清空。
- 会话 PIN 一键锁定；锁定后不显示按钮、不读取值、不执行填充。

## 项目目录

```text
job-autofill-extension/
├── manifest.json
├── defaults.js
├── background.js
├── content.js
├── content-style.js
├── content.css
├── options.html
├── options.js
├── options.css
├── assets/
│   ├── icon16.png
│   ├── icon32.png
│   ├── icon48.png
│   └── icon128.png
├── test/
│   ├── test-form.html
│   └── smart-match.test.js
└── README.md
```

## Chrome 安装步骤

1. 打开 `chrome://extensions`。
2. 开启右上角“开发者模式”。
3. 点击“加载已解压的扩展程序”。
4. 选择本项目目录，也就是包含 `manifest.json` 的文件夹。

## Edge 安装步骤

1. 打开 `edge://extensions`。
2. 开启“开发人员模式”。
3. 点击“加载解压缩的扩展”。
4. 选择本项目目录，也就是包含 `manifest.json` 的文件夹。

## 设置个人信息

1. 在扩展管理页打开本扩展的“扩展选项”，或在网页悬浮面板中点击“信息设置”。
2. 选择已有方案，或创建“英文简历”“技术岗位”“国企岗位”等新方案。
3. 点击“新增字段”或“编辑”，填写按钮名称、实际填充值、所属分组、敏感标记和可选格式变体。
4. 使用字段或分组右侧的上移、下移按钮调整顺序。
5. 所有修改会立即保存到本机。

默认模板只包含字段名称，所有实际值均为空。源代码中没有任何真实个人信息。

## 使用流程

1. 打开普通招聘网站或本项目的测试页面。
2. 点击浏览器工具栏中的扩展图标，右侧出现悬浮面板。
3. 点击网页中需要填写的输入框，确认它出现蓝色高亮。
4. 如果识别到高置信度字段，面板会自动滚动并高亮推荐按钮；也可以使用分组快速导航栏或搜索功能手动定位。
5. 点击对应信息按钮完成填写。智能匹配只负责定位，绝不会自动填值。
6. 看到“已填入：字段名”后检查页面值。
7. 如需恢复填写前的值，点击“撤销填写”。
8. 需要隐藏信息时点击“锁定面板”，设置 4～8 位数字临时 PIN。

扩展不会自动移动焦点、点击下一项、提交表单或绕过验证码。

## 测试页面

`test/test-form.html` 包含普通文本框、手机号、邮箱、URL、日期、月份、数字、textarea、contenteditable、原生 select、readonly、disabled、password、file、验证码、银行卡字段、动态渲染输入框和受控输入事件镜像。

有两种测试方式：

1. 直接在浏览器中打开 `test/test-form.html`。然后在扩展详情页开启“允许访问文件网址”。
2. 使用任意本地静态服务器打开该页面。静态服务器仅用于测试，不是扩展运行依赖。

建议检查：

1. 连续点击工具栏图标只显示或隐藏一个面板。
2. 面板可拖动、折叠、关闭，再次点击图标可重新显示。
3. 点击支持的控件后高亮和目标摘要正确。
4. 点击面板按钮不会丢失网页目标控件。
5. 点击手机号、邮箱、学校等标签明确的控件后，面板自动定位并标记相应字段；含糊或冲突场景不应强行推荐。
6. 点击分组快速导航后，相应分组展开并滚动到可见区域；手动滚动列表时当前分组标记同步更新。
7. 文本框填入后，测试页日志中出现 `beforeinput`、`input`、`change`。
8. “模拟受控文本框”的状态镜像随 `input` 事件更新。
9. 学历字段值为“本科”“硕士”等时，原生 select 精确选中。
10. password、file、readonly、disabled、验证码和银行卡字段给出拒绝原因。
11. 点击模拟提交按钮只触发测试页自身的拦截逻辑；扩展从不提交。
12. 刷新页面后原目标引用消失，需要重新选择输入框。

智能匹配的纯逻辑回归测试可在项目根目录执行：

```text
node test/smart-match.test.js
```

## 权限解释

扩展只申请以下权限：

- `activeTab`：仅在用户点击扩展图标后临时访问当前标签页。
- `scripting`：按需注入本地 `content.js`，用于显示面板和填写用户选中的控件。
- `storage`：在本机保存用户配置，并在当前浏览器会话中保存 PIN 摘要。

扩展没有 `<all_urls>`、浏览历史、Cookie、网络请求或下载权限，也没有任何 `host_permissions`。

## 隐私与安全说明

- 所有信息仅写入 `chrome.storage.local`，不使用 `chrome.storage.sync`。
- 不向服务器发送数据，不使用遥测、统计或埋点。
- 不在控制台打印字段值或 PIN。
- 敏感字段在面板中只显示掩码；真实值只在点击对应按钮时按字段读取。
- content script 不会一次性收到全部真实值。
- contenteditable 只写入纯文本，不插入 HTML。
- 扩展主动拒绝密码、文件上传、验证码、银行卡和支付类控件。
- 本地存储不是强加密。不要保存账号密码、银行卡号、CVV、支付密码、短信验证码或其他高价值凭证。
- JSON 导出文件包含完整配置，可能含敏感值。请只保存在可信设备和位置。

## 已知限制

以下场景无法可靠支持，扩展不会尝试绕过网站限制：

- 跨域 iframe 中的表单。
- closed Shadow DOM 内部控件。
- Canvas 绘制的自定义输入控件。
- 非原生自定义下拉框、日期选择器、级联选择器和富文本编辑器。
- 验证码、文件上传、密码、银行卡和支付控件。
- 浏览器内部页面，例如 `chrome://`、`edge://`。
- Chrome Web Store、Edge Add-ons 等禁止脚本注入的页面。
- 招聘网站主动阻止或重写输入事件时，填充可能失败。
- 会话 PIN 是轻量隐私遮挡，不是数据加密或账户认证机制。

## 常见问题

### 点击扩展图标没有显示面板

当前页面可能禁止扩展脚本注入。请确认不是 `chrome://`、`edge://`、扩展商店页面或受策略保护的页面。测试本地文件时还需在扩展详情页开启“允许访问文件网址”。

### 点击字段按钮提示“尚未设置”

打开“信息设置”，为当前方案中的字段填写实际值并保存。

### 下拉框提示“未找到对应选项”

原生 select 只接受精确匹配。请将字段值设置为 option 的实际 `value` 或完整显示文本。

### 日期没有按期望格式填写

在字段编辑框中配置格式变体，并在悬浮面板按钮旁选择格式。原生 `input[type=date]` 和 `input[type=month]` 会强制使用浏览器规定的 ISO 格式。

### 忘记会话 PIN

关闭并重新启动浏览器会话后，存储在 `chrome.storage.session` 中的 PIN 摘要会失效。PIN 不会以明文保存，无法找回。

### 网站使用 React / Vue，为什么仍然没有响应

扩展使用原生 setter 和标准冒泡事件，可覆盖常见受控输入实现；但网站仍可采用自定义状态机、事件拦截或非标准控件。此类页面只能由网站自身提供兼容接口。

## 后续可扩展功能

- 用户自定义字段快捷键。
- 用户可自定义字段匹配别名和禁用某个字段的智能匹配。
- 方案内字段复制与跨方案复制。
- 本地加密保险库与主密码派生密钥。
- 更多地区日期、电话和地址格式转换。
- 对开放 Shadow DOM 与同源 iframe 的可选支持。
- 自动化端到端测试和扩展商店打包流程。
